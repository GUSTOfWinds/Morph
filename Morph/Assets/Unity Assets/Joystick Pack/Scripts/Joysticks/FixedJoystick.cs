using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FixedJoystick : Joystick
{
	//player.transform.rotation = Quaternion.LookRotation(movementDirection).normalized;
	//player.Translate(movementDirection* speedMultiplier * Time.deltaTime, Space.World);
}